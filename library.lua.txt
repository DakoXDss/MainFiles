local CoreGui = game:GetService("CoreGui")
local TweenService = game:GetService("TweenService")
local UserInputService = game:GetService("UserInputService")
local RunService = game:GetService("RunService")

local Library = {}
local Utility = {}

local Colors = {
    MainBackground = Color3.fromRGB(5, 7, 10),
    SidebarBackground = Color3.fromRGB(15, 18, 24),
    SectionBackground = Color3.fromRGB(18, 22, 29),
    ElementBackground = Color3.fromRGB(24, 29, 38),
    AccentMatch = Color3.fromRGB(40, 100, 255),
    AccentOff = Color3.fromRGB(45, 50, 60),
    TextWhite = Color3.fromRGB(255, 255, 255),
    TextGray = Color3.fromRGB(160, 165, 175),
    TextDark = Color3.fromRGB(100, 105, 115)
}

function Utility:Tween(instance, properties, duration)
    local tween = TweenService:Create(instance, TweenInfo.new(duration or 0.2, Enum.EasingStyle.Sine, Enum.EasingDirection.Out), properties)
    tween:Play()
    return tween
end

function Utility:MakeDraggable(topbar, main)
    local dragging, dragInput, dragStart, startPos

    topbar.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 then
            dragging = true
            dragStart = input.Position
            startPos = main.Position
            
            input.Changed:Connect(function()
                if input.UserInputState == Enum.UserInputState.End then
                    dragging = false
                end
            end)
        end
    end)

    topbar.InputChanged:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseMovement then
            dragInput = input
        end
    end)

    UserInputService.InputChanged:Connect(function(input)
        if input == dragInput and dragging then
            local delta = input.Position - dragStart
            main.Position = UDim2.new(startPos.X.Scale, startPos.X.Offset + delta.X, startPos.Y.Scale, startPos.Y.Offset + delta.Y)
        end
    end)
end

function Library:CreateWindow(config)
    config = config or {}
    local Title = config.Title or "LUCID"

    local ScreenGui = Instance.new("ScreenGui")
    ScreenGui.Name = "LucidUI"
    ScreenGui.ResetOnSpawn = false
    -- Try to parent to CoreGui so it doesn't get messed up by game UI
    pcall(function() ScreenGui.Parent = CoreGui end)
    if not ScreenGui.Parent then ScreenGui.Parent = game.Players.LocalPlayer:WaitForChild("PlayerGui") end

    local MainFrame = Instance.new("Frame")
    MainFrame.Name = "MainFrame"
    MainFrame.Parent = ScreenGui
    MainFrame.BackgroundColor3 = Colors.MainBackground
    MainFrame.BorderSizePixel = 0
    MainFrame.Position = UDim2.new(0.5, -350, 0.5, -250)
    MainFrame.Size = UDim2.new(0, 750, 0, 520)

    local UICorner = Instance.new("UICorner")
    UICorner.CornerRadius = UDim.new(0, 6)
    UICorner.Parent = MainFrame

    -- Inner Shadow/Border effect
    local UIStroke = Instance.new("UIStroke")
    UIStroke.Color = Color3.fromRGB(30, 35, 45)
    UIStroke.Parent = MainFrame

    -- Sidebar
    local Sidebar = Instance.new("Frame")
    Sidebar.Name = "Sidebar"
    Sidebar.Parent = MainFrame
    Sidebar.BackgroundColor3 = Colors.SidebarBackground
    Sidebar.BorderSizePixel = 0
    Sidebar.Size = UDim2.new(0, 180, 1, 0)
    
    local SidebarCorner = Instance.new("UICorner")
    SidebarCorner.CornerRadius = UDim.new(0, 6)
    SidebarCorner.Parent = Sidebar

    local TitleLabel = Instance.new("TextLabel")
    TitleLabel.Name = "Title"
    TitleLabel.Parent = Sidebar
    TitleLabel.BackgroundTransparency = 1
    TitleLabel.Position = UDim2.new(0, 20, 0, 20)
    TitleLabel.Size = UDim2.new(1, -40, 0, 30)
    TitleLabel.Font = Enum.Font.GothamBold
    TitleLabel.Text = Title
    TitleLabel.TextColor3 = Colors.TextWhite
    TitleLabel.TextSize = 22
    TitleLabel.TextXAlignment = Enum.TextXAlignment.Left

    local TabContainer = Instance.new("ScrollingFrame")
    TabContainer.Name = "TabContainer"
    TabContainer.Parent = Sidebar
    TabContainer.BackgroundTransparency = 1
    TabContainer.Position = UDim2.new(0, 10, 0, 70)
    TabContainer.Size = UDim2.new(1, -20, 1, -140)
    TabContainer.ScrollBarThickness = 0

    local TabListLayout = Instance.new("UIListLayout")
    TabListLayout.Parent = TabContainer
    TabListLayout.SortOrder = Enum.SortOrder.LayoutOrder
    TabListLayout.Padding = UDim.new(0, 5)

    -- User Profile Area (Bottom Left)
    local UserArea = Instance.new("Frame")
    UserArea.Name = "UserArea"
    UserArea.Parent = Sidebar
    UserArea.BackgroundColor3 = Color3.fromRGB(12, 14, 18)
    UserArea.BorderSizePixel = 0
    UserArea.Position = UDim2.new(0, 0, 1, -60)
    UserArea.Size = UDim2.new(1, 0, 0, 60)
    
    local UserAreaCorner = Instance.new("UICorner")
    UserAreaCorner.CornerRadius = UDim.new(0, 6)
    UserAreaCorner.Parent = UserArea

    local Avatar = Instance.new("ImageLabel")
    Avatar.Name = "Avatar"
    Avatar.Parent = UserArea
    Avatar.BackgroundColor3 = Color3.fromRGB(40, 40, 50)
    Avatar.Position = UDim2.new(0, 15, 0.5, -15)
    Avatar.Size = UDim2.new(0, 30, 0, 30)
    Avatar.Image = "rbxassetid://4049446654" -- Placeholder generic circle
    Instance.new("UICorner", Avatar).CornerRadius = UDim.new(1, 0)
    
    -- In a real script we would grab the player's avatar
    pcall(function()
        Avatar.Image = "https://www.roblox.com/headshot-thumbnail/image?userId="..game.Players.LocalPlayer.UserId.."&width=420&height=420&format=png"
    end)

    local UserName = Instance.new("TextLabel")
    UserName.Parent = UserArea
    UserName.BackgroundTransparency = 1
    UserName.Position = UDim2.new(0, 55, 0, 12)
    UserName.Size = UDim2.new(1, -60, 0, 15)
    UserName.Font = Enum.Font.GothamMedium
    UserName.Text = game.Players.LocalPlayer and game.Players.LocalPlayer.Name or "evrope"
    UserName.TextColor3 = Colors.TextWhite
    UserName.TextSize = 13
    UserName.TextXAlignment = Enum.TextXAlignment.Left

    local UserSub = Instance.new("TextLabel")
    UserSub.Parent = UserArea
    UserSub.BackgroundTransparency = 1
    UserSub.Position = UDim2.new(0, 55, 0, 30)
    UserSub.Size = UDim2.new(1, -60, 0, 15)
    UserSub.Font = Enum.Font.Gotham
    UserSub.Text = "Till: Lifetime"
    UserSub.TextColor3 = Colors.AccentMatch
    UserSub.TextSize = 11
    UserSub.TextXAlignment = Enum.TextXAlignment.Left

    -- Make it draggable from Sidebar
    Utility:MakeDraggable(Sidebar, MainFrame)

    -- Topbar (Subtabs area & Save button)
    local Topbar = Instance.new("Frame")
    Topbar.Name = "Topbar"
    Topbar.Parent = MainFrame
    Topbar.BackgroundTransparency = 1
    Topbar.Position = UDim2.new(0, 180, 0, 0)
    Topbar.Size = UDim2.new(1, -180, 0, 60)
    
    local SaveButton = Instance.new("TextButton")
    SaveButton.Parent = Topbar
    SaveButton.BackgroundColor3 = Color3.fromRGB(15, 18, 24)
    SaveButton.Position = UDim2.new(0, 20, 0, 15)
    SaveButton.Size = UDim2.new(0, 90, 0, 30)
    SaveButton.Font = Enum.Font.Gotham
    SaveButton.Text = "  [s] Save"
    SaveButton.TextColor3 = Colors.TextGray
    SaveButton.TextSize = 12
    Instance.new("UICorner", SaveButton).CornerRadius = UDim.new(0, 4)
    Instance.new("UIStroke", SaveButton).Color = Color3.fromRGB(30, 35, 45)

    local SubtabContainer = Instance.new("Frame")
    SubtabContainer.Parent = Topbar
    SubtabContainer.BackgroundColor3 = Color3.fromRGB(12, 14, 18)
    SubtabContainer.Position = UDim2.new(0, 125, 0, 15)
    SubtabContainer.Size = UDim2.new(0, 300, 0, 30)
    Instance.new("UICorner", SubtabContainer).CornerRadius = UDim.new(0, 4)

    -- Content Area
    local ContentArea = Instance.new("Frame")
    ContentArea.Name = "ContentArea"
    ContentArea.Parent = MainFrame
    ContentArea.BackgroundTransparency = 1
    ContentArea.Position = UDim2.new(0, 180, 0, 60)
    ContentArea.Size = UDim2.new(1, -180, 1, -60)

    local Window = {
        Tabs = {},
        CurrentTab = nil
    }

    function Window:CreateTab(name, icon)
        local TabBtn = Instance.new("TextButton")
        TabBtn.Parent = TabContainer
        TabBtn.BackgroundColor3 = Colors.SidebarBackground
        TabBtn.Size = UDim2.new(1, 0, 0, 32)
        TabBtn.Font = Enum.Font.Gotham
        TabBtn.Text = "   " .. name
        TabBtn.TextColor3 = Colors.TextGray
        TabBtn.TextSize = 13
        TabBtn.TextXAlignment = Enum.TextXAlignment.Left
        TabBtn.AutoButtonColor = false
        Instance.new("UICorner", TabBtn).CornerRadius = UDim.new(0, 4)

        local TabContent = Instance.new("Frame")
        TabContent.Name = name.."_Content"
        TabContent.Parent = ContentArea
        TabContent.BackgroundTransparency = 1
        TabContent.Size = UDim2.new(1, 0, 1, 0)
        TabContent.Visible = false

        -- Two columns layout like Neverlose
        local LeftCol = Instance.new("ScrollingFrame")
        LeftCol.Parent = TabContent
        LeftCol.BackgroundTransparency = 1
        LeftCol.Position = UDim2.new(0, 15, 0, 10)
        LeftCol.Size = UDim2.new(0.5, -25, 1, -20)
        LeftCol.ScrollBarThickness = 0
        local LeftList = Instance.new("UIListLayout")
        LeftList.Parent = LeftCol
        LeftList.SortOrder = Enum.SortOrder.LayoutOrder
        LeftList.Padding = UDim.new(0, 10)

        local RightCol = Instance.new("ScrollingFrame")
        RightCol.Parent = TabContent
        RightCol.BackgroundTransparency = 1
        RightCol.Position = UDim2.new(0.5, 5, 0, 10)
        RightCol.Size = UDim2.new(0.5, -20, 1, -20)
        RightCol.ScrollBarThickness = 0
        local RightList = Instance.new("UIListLayout")
        RightList.Parent = RightCol
        RightList.SortOrder = Enum.SortOrder.LayoutOrder
        RightList.Padding = UDim.new(0, 10)

        TabBtn.MouseButton1Click:Connect(function()
            for _, tab in pairs(Window.Tabs) do
                tab.Content.Visible = false
                Utility:Tween(tab.Button, {BackgroundColor3 = Colors.SidebarBackground, TextColor3 = Colors.TextGray}, 0.2)
            end
            TabContent.Visible = true
            Utility:Tween(TabBtn, {BackgroundColor3 = Color3.fromRGB(20, 24, 32), TextColor3 = Colors.TextWhite}, 0.2)
        end)

        local TabObj = {
            Button = TabBtn,
            Content = TabContent,
            Left = LeftCol,
            Right = RightCol
        }
        
        table.insert(Window.Tabs, TabObj)
        if #Window.Tabs == 1 then
            TabContent.Visible = true
            Utility:Tween(TabBtn, {BackgroundColor3 = Color3.fromRGB(20, 24, 32), TextColor3 = Colors.TextWhite}, 0.2)
        end

        local TabMethods = {}

        function TabMethods:CreateSection(options)
            options = options or {}
            local secName = options.Name or "Section"
            local side = options.Side or "Left"

            local SectionFrame = Instance.new("Frame")
            SectionFrame.Parent = side == "Left" and LeftCol or RightCol
            SectionFrame.BackgroundColor3 = Colors.SectionBackground
            SectionFrame.Size = UDim2.new(1, 0, 0, 0) -- Auto adjusts
            SectionFrame.AutomaticSize = Enum.AutomaticSize.Y
            Instance.new("UICorner", SectionFrame).CornerRadius = UDim.new(0, 4)
            Instance.new("UIStroke", SectionFrame).Color = Color3.fromRGB(26, 30, 38)

            local SectionPadding = Instance.new("UIPadding")
            SectionPadding.Parent = SectionFrame
            SectionPadding.PaddingTop = UDim.new(0, 10)
            SectionPadding.PaddingBottom = UDim.new(0, 15)
            SectionPadding.PaddingLeft = UDim.new(0, 15)
            SectionPadding.PaddingRight = UDim.new(0, 15)

            local SecLayout = Instance.new("UIListLayout")
            SecLayout.Parent = SectionFrame
            SecLayout.SortOrder = Enum.SortOrder.LayoutOrder
            SecLayout.Padding = UDim.new(0, 12)

            local SecTitle = Instance.new("TextLabel")
            SecTitle.Parent = SectionFrame
            SecTitle.BackgroundTransparency = 1
            SecTitle.Size = UDim2.new(1, 0, 0, 20)
            SecTitle.Font = Enum.Font.GothamMedium
            SecTitle.Text = secName
            SecTitle.TextColor3 = Colors.TextWhite
            SecTitle.TextSize = 13
            SecTitle.TextXAlignment = Enum.TextXAlignment.Left

            local Elements = {}

            function Elements:CreateToggle(tConfig)
                local tName = tConfig.Name or "Toggle"
                local callback = tConfig.Callback or function() end
                local state = tConfig.Default or false

                local ToggleFrame = Instance.new("Frame")
                ToggleFrame.Parent = SectionFrame
                ToggleFrame.BackgroundTransparency = 1
                ToggleFrame.Size = UDim2.new(1, 0, 0, 20)

                local TitleLabel = Instance.new("TextLabel")
                TitleLabel.Parent = ToggleFrame
                TitleLabel.BackgroundTransparency = 1
                TitleLabel.Size = UDim2.new(1, -40, 1, 0)
                TitleLabel.Font = Enum.Font.Gotham
                TitleLabel.Text = tName
                TitleLabel.TextColor3 = Colors.TextGray
                TitleLabel.TextSize = 13
                TitleLabel.TextXAlignment = Enum.TextXAlignment.Left

                local ToggleSwitch = Instance.new("TextButton")
                ToggleSwitch.Parent = ToggleFrame
                ToggleSwitch.BackgroundColor3 = state and Colors.AccentMatch or Colors.AccentOff
                ToggleSwitch.Position = UDim2.new(1, -30, 0.5, -7)
                ToggleSwitch.Size = UDim2.new(0, 30, 0, 14)
                ToggleSwitch.Text = ""
                Instance.new("UICorner", ToggleSwitch).CornerRadius = UDim.new(1, 0)

                local ToggleCircle = Instance.new("Frame")
                ToggleCircle.Parent = ToggleSwitch
                ToggleCircle.BackgroundColor3 = Colors.TextWhite
                ToggleCircle.Position = state and UDim2.new(0, 17, 0.5, -5) or UDim2.new(0, 3, 0.5, -5)
                ToggleCircle.Size = UDim2.new(0, 10, 0, 10)
                Instance.new("UICorner", ToggleCircle).CornerRadius = UDim.new(1, 0)

                ToggleSwitch.MouseButton1Click:Connect(function()
                    state = not state
                    Utility:Tween(ToggleSwitch, {BackgroundColor3 = state and Colors.AccentMatch or Colors.AccentOff}, 0.2)
                    Utility:Tween(ToggleCircle, {Position = state and UDim2.new(0, 17, 0.5, -5) or UDim2.new(0, 3, 0.5, -5)}, 0.2)
                    Utility:Tween(TitleLabel, {TextColor3 = state and Colors.TextWhite or Colors.TextGray}, 0.2)
                    callback(state)
                end)
            end

            function Elements:CreateSlider(sConfig)
                local sName = sConfig.Name or "Slider"
                local min = sConfig.Min or 0
                local max = sConfig.Max or 100
                local default = sConfig.Default or min
                local callback = sConfig.Callback or function() end

                local SliderFrame = Instance.new("Frame")
                SliderFrame.Parent = SectionFrame
                SliderFrame.BackgroundTransparency = 1
                SliderFrame.Size = UDim2.new(1, 0, 0, 30)

                local TitleLabel = Instance.new("TextLabel")
                TitleLabel.Parent = SliderFrame
                TitleLabel.BackgroundTransparency = 1
                TitleLabel.Size = UDim2.new(1, 0, 0, 15)
                TitleLabel.Font = Enum.Font.Gotham
                TitleLabel.Text = sName
                TitleLabel.TextColor3 = Colors.TextGray
                TitleLabel.TextSize = 13
                TitleLabel.TextXAlignment = Enum.TextXAlignment.Left

                local ValLabel = Instance.new("TextLabel")
                ValLabel.Parent = SliderFrame
                ValLabel.BackgroundTransparency = 1
                ValLabel.Position = UDim2.new(1, -50, 0, 0)
                ValLabel.Size = UDim2.new(0, 50, 0, 15)
                ValLabel.Font = Enum.Font.GothamBold
                ValLabel.Text = tostring(default) .. "%"
                ValLabel.TextColor3 = Colors.TextWhite
                ValLabel.TextSize = 12
                ValLabel.TextXAlignment = Enum.TextXAlignment.Right

                local Track = Instance.new("Frame")
                Track.Parent = SliderFrame
                Track.BackgroundColor3 = Colors.ElementBackground
                Track.Position = UDim2.new(0, 0, 0, 22)
                Track.Size = UDim2.new(1, 0, 0, 6)
                Instance.new("UICorner", Track).CornerRadius = UDim.new(1, 0)

                local Fill = Instance.new("Frame")
                Fill.Parent = Track
                Fill.BackgroundColor3 = Colors.AccentMatch
                Fill.Size = UDim2.new((default-min)/(max-min), 0, 1, 0)
                Instance.new("UICorner", Fill).CornerRadius = UDim.new(1, 0)

                local function UpdateSlider(input)
                    local pos = math.clamp((input.Position.X - Track.AbsolutePosition.X) / Track.AbsoluteSize.X, 0, 1)
                    local value = math.floor(min + ((max - min) * pos))
                    Utility:Tween(Fill, {Size = UDim2.new(pos, 0, 1, 0)}, 0.05)
                    ValLabel.Text = tostring(value) .. "%"
                    callback(value)
                end

                Track.InputBegan:Connect(function(input)
                    if input.UserInputType == Enum.UserInputType.MouseButton1 then
                        UpdateSlider(input)
                        local moveCon
                        local releaseCon
                        moveCon = UserInputService.InputChanged:Connect(function(change)
                            if change.UserInputType == Enum.UserInputType.MouseMovement then
                                UpdateSlider(change)
                            end
                        end)
                        releaseCon = UserInputService.InputEnded:Connect(function(release)
                            if release.UserInputType == Enum.UserInputType.MouseButton1 then
                                moveCon:Disconnect()
                                releaseCon:Disconnect()
                            end
                        end)
                    end
                end)
            end

            function Elements:CreateDropdown(dConfig)
                local dName = dConfig.Name or "Dropdown"
                local options = dConfig.Options or {}
                local default = dConfig.Default or options[1]
                local callback = dConfig.Callback or function() end

                local DropdownFrame = Instance.new("Frame")
                DropdownFrame.Parent = SectionFrame
                DropdownFrame.BackgroundTransparency = 1
                DropdownFrame.Size = UDim2.new(1, 0, 0, 25)

                local TitleLabel = Instance.new("TextLabel")
                TitleLabel.Parent = DropdownFrame
                TitleLabel.BackgroundTransparency = 1
                TitleLabel.Size = UDim2.new(0, 80, 1, 0)
                TitleLabel.Font = Enum.Font.Gotham
                TitleLabel.Text = dName
                TitleLabel.TextColor3 = Colors.TextGray
                TitleLabel.TextSize = 13
                TitleLabel.TextXAlignment = Enum.TextXAlignment.Left

                local Button = Instance.new("TextButton")
                Button.Parent = DropdownFrame
                Button.BackgroundColor3 = Colors.ElementBackground
                Button.Position = UDim2.new(0, 90, 0, 0)
                Button.Size = UDim2.new(1, -90, 1, 0)
                Button.Font = Enum.Font.Gotham
                Button.Text = "  " .. tostring(default)
                Button.TextColor3 = Colors.TextWhite
                Button.TextSize = 12
                Button.TextXAlignment = Enum.TextXAlignment.Left
                Instance.new("UICorner", Button).CornerRadius = UDim.new(0, 4)
                Instance.new("UIStroke", Button).Color = Color3.fromRGB(30, 35, 40)
                
                -- Simple dropdown arrow placeholder
                local Arrow = Instance.new("TextLabel")
                Arrow.Parent = Button
                Arrow.BackgroundTransparency = 1
                Arrow.Position = UDim2.new(1, -20, 0, 0)
                Arrow.Size = UDim2.new(0, 20, 1, 0)
                Arrow.Font = Enum.Font.Gotham
                Arrow.Text = "v"
                Arrow.TextColor3 = Colors.TextGray
                Arrow.TextSize = 12
            end

            function Elements:CreateInput(iConfig)
                local iName = iConfig.Name or "Input"
                local placeHolder = iConfig.Placeholder or ""
                local callback = iConfig.Callback or function() end

                local InputFrame = Instance.new("Frame")
                InputFrame.Parent = SectionFrame
                InputFrame.BackgroundTransparency = 1
                InputFrame.Size = UDim2.new(1, 0, 0, 25)

                local TitleLabel = Instance.new("TextLabel")
                TitleLabel.Parent = InputFrame
                TitleLabel.BackgroundTransparency = 1
                TitleLabel.Size = UDim2.new(0, 80, 1, 0)
                TitleLabel.Font = Enum.Font.Gotham
                TitleLabel.Text = iName
                TitleLabel.TextColor3 = Colors.TextGray
                TitleLabel.TextSize = 13
                TitleLabel.TextXAlignment = Enum.TextXAlignment.Left

                local TextBox = Instance.new("TextBox")
                TextBox.Parent = InputFrame
                TextBox.BackgroundColor3 = Colors.ElementBackground
                TextBox.Position = UDim2.new(0, 90, 0, 0)
                TextBox.Size = UDim2.new(1, -90, 1, 0)
                TextBox.Font = Enum.Font.Gotham
                TextBox.Text = ""
                TextBox.PlaceholderText = placeHolder
                TextBox.TextColor3 = Colors.TextWhite
                TextBox.TextSize = 12
                TextBox.TextXAlignment = Enum.TextXAlignment.Left
                Instance.new("UICorner", TextBox).CornerRadius = UDim.new(0, 4)
                Instance.new("UIStroke", TextBox).Color = Color3.fromRGB(30, 35, 40)
                
                -- Need a UIPadding for internal text spacing
                local Padding = Instance.new("UIPadding")
                Padding.Parent = TextBox
                Padding.PaddingLeft = UDim.new(0, 8)

                TextBox.FocusLost:Connect(function(enterPressed)
                    callback(TextBox.Text)
                end)
            end

            function Elements:CreateColorPicker(cConfig)
                local cName = cConfig.Name or "Color Picker"
                local default = cConfig.Default or Color3.fromRGB(255, 255, 255)
                local callback = cConfig.Callback or function() end

                local ColorFrame = Instance.new("Frame")
                ColorFrame.Parent = SectionFrame
                ColorFrame.BackgroundTransparency = 1
                ColorFrame.Size = UDim2.new(1, 0, 0, 20)

                local TitleLabel = Instance.new("TextLabel")
                TitleLabel.Parent = ColorFrame
                TitleLabel.BackgroundTransparency = 1
                TitleLabel.Size = UDim2.new(1, -30, 1, 0)
                TitleLabel.Font = Enum.Font.Gotham
                TitleLabel.Text = cName
                TitleLabel.TextColor3 = Colors.TextGray
                TitleLabel.TextSize = 13
                TitleLabel.TextXAlignment = Enum.TextXAlignment.Left

                local ColorDisplay = Instance.new("Frame")
                ColorDisplay.Parent = ColorFrame
                ColorDisplay.BackgroundColor3 = default
                ColorDisplay.Position = UDim2.new(1, -20, 0.5, -7)
                ColorDisplay.Size = UDim2.new(0, 20, 0, 14)
                Instance.new("UICorner", ColorDisplay).CornerRadius = UDim.new(1, 0)
                Instance.new("UIStroke", ColorDisplay).Color = Color3.fromRGB(30, 35, 40)
            end

            function Elements:CreateButton(bConfig)
                local bName = bConfig.Name or "Button"
                local callback = bConfig.Callback or function() end

                local BtnFrame = Instance.new("Frame")
                BtnFrame.Parent = SectionFrame
                BtnFrame.BackgroundTransparency = 1
                BtnFrame.Size = UDim2.new(1, 0, 0, 25)

                local Button = Instance.new("TextButton")
                Button.Parent = BtnFrame
                Button.BackgroundColor3 = Colors.ElementBackground
                Button.Size = UDim2.new(1, 0, 1, 0)
                Button.Font = Enum.Font.GothamMedium
                Button.Text = bName
                Button.TextColor3 = Colors.TextGray
                Button.TextSize = 12
                Instance.new("UICorner", Button).CornerRadius = UDim.new(0, 4)
                Instance.new("UIStroke", Button).Color = Color3.fromRGB(30, 35, 40)

                Button.MouseButton1Click:Connect(function()
                    callback()
                end)
            end

            return Elements
        end

        return TabMethods
    end

    return Window
end

return Library
